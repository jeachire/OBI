% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getPositive.R
\name{getPositive}
\alias{getPositive}
\title{Identify Positive Parameter Constraints}
\usage{
getPositive(dist)
}
\arguments{
\item{dist}{A character string specifying the distribution name.
Supported options include: \code{"normal"}, \code{"poisson"},
\code{"exponential"}, \code{"gamma"}, and \code{"weibull"}.}
}
\value{
A logical vector of length equal to the number of parameters
of the specified distribution. Each element indicates whether the
corresponding parameter is restricted to \eqn{(0, \infty)}
(\code{TRUE}) or not (\code{FALSE}).
}
\description{
Returns a logical vector indicating which parameters of a given
distribution are restricted to the positive real line \eqn{(0, \infty)}.
}
\details{
\itemize{
\item \code{"normal"}: parameters = mean, sd → \code{c(FALSE, TRUE)}
\item \code{"poisson"}: parameter = lambda → \code{c(TRUE)}
\item \code{"exponential"}: parameter = rate → \code{c(TRUE)}
\item \code{"gamma"}: parameters = shape, rate → \code{c(TRUE, TRUE)}
\item \code{"weibull"}: parameters = shape, scale → \code{c(TRUE, TRUE)}
}

If an unsupported distribution is provided, the function throws an error.
}
\examples{
getPositive("normal")
getPositive("gamma")
getPositive("weibull")

}
